import { Animate, Transition } from "@/types/animate";

const PickAnimation = {
  whileTap: {
    scale: 0.98,
  } as Animate,

  transition: {
    duration: 0.1,
  } as Transition,
};

export default PickAnimation;
